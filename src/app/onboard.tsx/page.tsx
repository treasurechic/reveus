import { MainLayout } from "@/components/mainLayout";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Reveus - Onboarding",
};

const Onboard = () => {
  return <MainLayout>Onboard</MainLayout>;
};

export default Onboard;
